package controllers;

public class shotController {
  //class where assign the button to the type of weapon available per ship type.
	//takes in?
	//uses the scenebuilder of buttons in grid scene
  
}
